<div class="users-index">
  <h1>ユーザー一覧</h1>

  <%= will_paginate @users, previous_label: '← Previous', next_label: 'Next →' %>
  
  <ul class="users mt-3">
    <%= render @users %>
  </ul>

  <%= will_paginate @users, previous_label: '← Previous', next_label: 'Next →' %>
</div>
